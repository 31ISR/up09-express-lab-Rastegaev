info:
  name: admin user
  type: http
  seq: 11

http:
  method: GET
  url: "{{bar}}api/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
