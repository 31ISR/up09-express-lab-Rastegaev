info:
  name: deletereviews
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{bar}}api/reviews/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
