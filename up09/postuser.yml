info:
  name: postuser
  type: http
  seq: 3

http:
  method: POST
  url: "{{bar}}api/auth/login"
  body:
    type: json
    data: |-
      {

        "email":"admin@gmail.com",
        "password":"qwerty123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
