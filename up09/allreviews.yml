info:
  name: allreviews
  type: http
  seq: 9

http:
  method: GET
  url: "{{bar}}api/books/:id/reviews"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
