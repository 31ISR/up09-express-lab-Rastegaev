info:
  name: newreviews
  type: http
  seq: 8

http:
  method: POST
  url: "{{bar}}api/books/:id/reviews"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "rating":4,
        "comment":"very coooool"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
