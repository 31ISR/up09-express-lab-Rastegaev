info:
  name: delte
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{bar}}api/books/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
