info:
  name: post register
  type: http
  seq: 4

http:
  method: POST
  url: "{{bar}}api/auth/register"
  body:
    type: json
    data: |-
      {
        "username":"admin",
        "email":"admin@gmail.com",
        "password":"qwerty123",
        "role":"admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
